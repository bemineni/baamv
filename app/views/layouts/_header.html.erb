<div class="red-line"></div>
<div id="nav-bar" class="gradient"></div>

<div id="container">
  <img id="logo" src= <%= asset_path('banner.png') %> width="342" height="100"></img>
  <div id="izmenu"> 
    <ul>
      <li class="menu-heading first"><%= link_to(raw("<i class='icon-home'></i>home") , root_path) %></li>
      <% if is_signedin? %>
          <li class="menu-heading last-item last"><%= link_to(raw("<i class='icon-user'></i>login"),login_path ) %></li>
      <% else %>
          <li class="menu-heading last-item last"><a href="#"><i class="icon-user"></i><%= "#{current_user.username}" %></a>
            <ul class="popup-menu">
              <li><%= link_to(raw("<i class='icon-wrench'></i>Settings") , current_user  )%> </li>
              <li class="last-item"><%= link_to(raw("<i class='icon-remove-sign'></i>logout"), logout_path, :method => :delete)%> </li>
            </ul>
          </li>
      <% end %>
    </ul>
  </div>


               